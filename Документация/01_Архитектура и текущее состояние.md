# Архитектура и текущее состояние GigaChat Ultra Local Agent

## Статус документа

Этот документ описывает **фактическое текущее состояние** проекта `ag_llm / GigaChat Ultra Local Agent`.

Он должен отражать то, что реально существует в коде и было подтверждено тестами или живым runtime.

Будущие функции здесь явно отделяются от реализованных.

Это краткая каноническая карта работающей архитектуры: для каждой крупной подсистемы здесь фиксируются её фактическое состояние и назначение, а подробный lifecycle, компоненты, состояния, правила и ограничения описываются в профильном архитектурном документе. При наличии профильного документа раздел этой карты должен явно указывать на него, не дублируя большое техническое описание.

После значимого изменения runtime этот документ проходит Documentation Impact Check вместе с профильным документом темы: актуальный код, diff, tests/checks и доступное live evidence определяют, нужно ли обновить краткий статус и устранить противоречащие текущие формулировки. Исторические записи не переписываются; если фактического влияния нет, искусственная правка не нужна. Маршрут выбора профильного документа для каждой TASK закреплён в `08_Старт.md`.

Документационная библиотека подготовленных текстов контекста перечислена в `16_Список_контекстов.md` (включая references `14` и `15`). Она сопровождается человеком и не подключается сервером к LLM автоматически. Фактическая runtime архитектура Workspace / Chat / Context описана ниже и в `03_Чаты, проекты и контекст.md`.

## Короткая карта CURRENT STATUS

V.6 V1: normal UI использует отдельный tool-free Planner (`planner_runtime.py`) и серверный Task Plan / Persistence Contract (`task_planner.py`), атомарно сохранённый вне Workspace. План заранее задаёт обязательства stages; PERSIST_REQUIRED снимается только по target/readback/postcondition evidence. Recovery и replan ограничены; Dredd отдельно маршрутизирует EXECUTION_DEFECT / PLAN_DEFECT / UNKNOWN. Прямые legacy callers сохраняют `planner_enabled=False`; ошибки включённого Planner не имеют fallback. Профильный владелец — `13`, назначения и изоляция ролей — `11`. Подтверждено automated tests и UI smoke, live GigaChat RUN ещё требуется.

| Подсистема | Что работает / ключевой инвариант | Профильный владелец |
| --- | --- | --- |
| Workspace, Chat и Context | Persistent RAW history, Project Context и активные RAW / SUMMARY представления; RAW остаётся исходником | `03_Чаты, проекты и контекст.md` |
| Model Assignment | Registry и отдельные назначения Main Chat, Compressor, Verifier; модель не владеет Agent Runtime | `11_Мультимодельная архитектура и назначение LLM.md` |
| Compression | Proposal под контролем пользователя и текущий одношаговый Compressor с soft guard; трёхвызовный pipeline и Review Chat ещё не работают | `Архитектурное решение - управляемое сжатие контекста.md` |
| Execution verification | Final Audit требует valid PASS; V.5 ограничивает correction handoffs двумя; permission escalation реализована; mutation intent — диагностический | `13_Архитектура оперативной верификации и контроля выполнения задач.md` |
| Voice / ASR | Production runtime отсутствует | `12_Архитектура голосового ввода и распознавания речи.md` |

Разделы ниже содержат оставшиеся проверенные факты и инварианты. Для cold start после этой карты читать прежде всего профиль текущей TASK по `08_Старт.md`; детали других веток не являются обязательным чтением.

Связанные архитектурные решения:

- [[00_Главная - карта проекта]]
- 03_Чаты, проекты и контекст
- 04_Дорожная карта разработки
- 05_Реестр задач
- 08_Старт
- 10_рабочая микро_память_Ultra(концепт)
- 11_Мультимодельная архитектура и назначение LLM
- 12_Архитектура голосового ввода и распознавания речи
- 13_Архитектура оперативной верификации и контроля выполнения задач
- Архитектурное решение - управляемое сжатие контекста

---

# 1. Общая архитектура

Проект больше не является системой, жёстко привязанной к одной модели `GigaChat-3-Ultra`.

Текущая архитектура развивается как локальный Agent Runtime, в котором модель является заменяемым вычислительным компонентом.

Упрощённая схема:

```text
USER
↓
Ultra UI
↓
AGENT RUNTIME
↓
MODEL ASSIGNMENT
↓
MODEL REGISTRY
↓
PROVIDER
↓
SELECTED MODEL
```

При этом:

```text
MODEL ≠ AGENT
```

Модель не является владельцем:

```text
Workspace
Chat
Context
Tools
Permissions
Backup
Verification
Trace
State
Lifecycle
```

Эти сущности принадлежат runtime.

---

# 2. Основные компоненты

Текущий runtime состоит из нескольких основных модулей.

## server.py

Backend Agent Runtime.

File toolbox включает READ `list_dir` / `read_file` / `find_text` / `read_file_range` и MUTATION `write_file` / `replace_text` / `insert_before` / `insert_after` / `delete_file`. Precise edit проверяет `expected_content_sha256` и единственность якоря до mutation; stale или ambiguous запрос не меняет файл. Практический порядок для Executor закреплён в `09_Обязательный регламент Ultra.md`.

Отвечает за:

```text
GigaChat API
MAIN CHAT RUN
file-agent tools
permissions
backup
verification
trace
Workspace interaction
Chat context loading
selected model execution
Independent Final Audit before SUCCESS
Final Audit Evidence collection
Evidence Completeness Gate
bounded same-RUN Final Audit correction V.5
structured Server Final Verifier Feedback
```

Для MAIN CHAT runtime выбирает модель через MODEL REGISTRY и передаёт фактический `provider_model_id` в API.

---

## ultra_ui.py

Основной пользовательский интерфейс.

Текущий UI реализован на:

```text
Tkinter
```

UI включает:

```text
trace / log
CHAT
message input
Workspace sidebar
Chat sidebar
MAIN CHAT model selector
VERIFIER MODEL selector
Compressor panel
Project Context editor
Role Context editors
message context actions
Compression Proposal UI
Original Viewer
```

---

## context_storage.py

Центральный storage API для Workspace и Chat.

Отвечает за:

```text
workspace identity
chat identity
message identity
RAW HISTORY
Project Context
Project Context revisions
MESSAGE CONTEXT STATE
Context Variants
active Working Representation
atomic storage operations
```

Обычные file-agent tools не должны напрямую изменять внутреннее `.ultra` storage.

---

## model_registry.py

Единый MODEL REGISTRY.

Он хранит описание доступных моделей независимо от конкретной роли.

Текущий ModelSpec включает:

```text
model_id
display_name
provider
provider_model_id
enabled
is_local
```

Текущий GigaChat registry содержит несколько моделей:

```text
GigaChat 2
GigaChat 2 Max
GigaChat 2 Pro
GigaChat 3 Lightning
GigaChat 3 Pro
GigaChat 3 Ultra
```

---

## gigachat_transport.py

Небольшой shared low-level слой GigaChat auth / transport.

Он содержит общие GigaChat endpoints и получение access token, чтобы `server.py` и `verifier_runtime.py` не зависели друг от друга через transport implementation.

Фактическое направление dependency:

```text
server.py
    ↓
verifier_runtime.py
    ↓
gigachat_transport.py
```

Обратной зависимости `verifier_runtime.py → server.py` нет.

Это пока не универсальный Provider Adapter.

---

## verifier_runtime.py

Отдельная runtime role Independent Verifier.

Текущие свойства:

```text
one-shot clean call
separate Verifier Model Assignment
default = GigaChat 3 Pro (`gigachat_3_pro`)
model resolution через MODEL REGISTRY
TOOLS = NONE
persistent Verifier Chat = NONE
Workspace / Chat Storage самостоятельно не создаёт и не читает
Executor conversation/session state не наследует
```

Verifier получает только явно переданные RAW TASK и `verification_context`, возвращает typed `VerifierResult` и поддерживает semantic `PASS` / `FAIL` для `PREFLIGHT`, `MUTATION`, `FINAL`, `CONSISTENCY`, `PERMISSION`. В текущем `run_agent_task(...)` подключены `FINAL` и `PERMISSION`; остальные типы доступны в protocol, но не являются активными production gates.

`VerifierRuntimeError` и `VerifierProtocolError` остаются отдельными от semantic FAIL. Malformed response не превращается в PASS.

---

## compressor_runtime.py

Отдельный runtime Context Compressor.

Он не является частью обычного MAIN CHAT RUN.

Compressor имеет собственную:

```text
роль
модель
контекст роли
prompt configuration
runtime target
soft guard
```

Compressor не получает file-agent tools.

---

## compressor_settings.py

Persistent настройки prompt-слоя Compressor.

Используются:

```text
Message Compression Template
Final Check Template
```

Поддерживаются placeholders:

```text
{{REDUCTION_PERCENT}}
{{REMAINING_PERCENT}}
```

---

## agent_global_context.py

Хранилище локального app-level Role Context.

Поддерживает отдельные agent/role IDs.

Например:

```text
ultra
compressor
```

Role Context хранится вне Workspace.

---

## workspace_runtime_settings.py

Хранит machine-local runtime-настройки Workspace.

Например:

```text
backup directory
run log directory
```

Эти настройки не должны смешиваться с portable `.ultra` state.

---

## ui_state.py

Persistent UI state.

Хранит:

```text
geometry
layout
theme
colors
Workspace registry
selected MAIN CHAT model
selected VERIFIER model
другие локальные UI settings
```

---

## server_context_messages.py

Отдельная подсистема server/runtime context messages.

Она не является заменой:

```text
Project Context
Role Context
Chat history
MESSAGE CONTEXT
```

Это отдельный служебный механизм.

В нём зарегистрировано active runtime event `final_audit.feedback`. Его human-readable template поддерживает built-in default, пользовательский override и fallback при некорректном override. Authoritative `SERVER FACTS — NOT TEMPLATE CONTROLLED` формируются отдельно в `server.py` и не принадлежат редактируемому template.

---

# 3. Workspace

Workspace является корнем конкретного проекта.

В Workspace хранится переносимое внутреннее состояние:

```text
.ultra/
```

Каждый Workspace имеет постоянный:

```text
workspace_id
```

Несколько Workspace могут быть зарегистрированы в UI.

Реализованы:

```text
создание Workspace
подключение существующего Workspace
переключение
переименование
удаление из UI registry
```

Удаление Workspace из UI registry не означает удаление самого проекта.

---

# 4. Portable и local state

Архитектурно разделяются:

```text
PORTABLE WORKSPACE STATE
```

и:

```text
LOCAL MACHINE STATE
```

Portable:

```text
.ultra/
```

Local:

```text
UI settings
backup paths
run log paths
Role Context
runtime configuration
```

Это позволяет переносить проект между компьютерами без смешивания machine-specific настроек.

---

# 5. Chat

Один Workspace может содержать несколько независимых Chat.

Каждый Chat имеет:

```text
chat_id
```

Реализованы:

```text
создание
переключение
переименование
persistent history
restart recovery
изоляция Chat history
```

Удаление Chat пока не является законченной production-функцией.

---

# 6. RAW HISTORY

Каждое сообщение Chat сохраняется как отдельный RAW MESSAGE.

Identity:

```text
workspace_id
chat_id
message_id
```

RAW MESSAGE содержит:

```text
original_text
```

RAW является immutable source of truth.

После создания RAW не должен изменяться из-за:

```text
compression
manual summary edit
restore
model replacement
UI rerender
```

---

# 7. Assistant provenance

Для assistant messages сохраняется producer metadata.

Например:

```text
kind
role_id
run_id
model_id
model_display_name
provider
provider_model_id
```

Это позволяет после restart понимать, какой именно моделью был создан конкретный ответ.

Старые сообщения без producer metadata остаются совместимыми.

---

# 8. Project Context

Каждый Workspace имеет общий:

```text
PROJECT CONTEXT
```

Он хранится в:

```text
.ultra/project_context.md
```

Project Context:

```text
общий для Chat одного Workspace
persistent
редактируется через UI
имеет историю ревизий
загружается runtime
```

Project Context не является Chat History.

---

# 9. Role Context

MAIN CHAT и COMPRESSOR имеют отдельные Role Context.

Пример:

```text
MAIN CHAT ROLE CONTEXT
COMPRESSOR ROLE CONTEXT
```

Role Context описывает роль.

Project Context описывает проект.

Chat Context описывает конкретный разговор.

Эти сущности не должны смешиваться.

---

# 10. MAIN CHAT MODEL ASSIGNMENT

MAIN CHAT больше не использует только одну жёстко заданную модель.

В UI реализован выбор модели MAIN CHAT.

Runtime получает:

```text
model_id
```

и разрешает его через:

```text
MODEL REGISTRY
```

в:

```text
provider
provider_model_id
```

Фактическая модель конкретного RUN фиксируется в trace.

---

# 11. GigaChat Multi-Model

Работа нескольких GigaChat-моделей через существующий backend подтверждена практически.

То есть этап:

```text
MODEL REGISTRY
+
GigaChat Multi-Model
+
MAIN CHAT Model Assignment
```

уже реализован.

Архитектура больше не зависит от одной `GigaChat-3-Ultra`.

---

# 12. Compressor Model Assignment

COMPRESSOR имеет отдельный выбор модели.

Например:

```text
MAIN CHAT
    GigaChat 3 Ultra

COMPRESSOR
    GigaChat 3 Pro
```

Модель Compressor выбирается независимо от MAIN CHAT.

Оба selector используют общий MODEL REGISTRY.

---

# 13. Изоляция ролей

MAIN CHAT, COMPRESSOR, PLANNER и VERIFIER являются разными runtime roles. Planner имеет отдельный `planner_model_id` (default `gigachat_ultra`) и clean bounded context.

Их вызовы не должны автоматически наследовать скрытую provider-side историю друг друга.

Каждый вызов получает явно собранный context.

Основной принцип:

```text
ROLE CONTEXT
+
ALLOWED SHARED CONTEXT
+
RUN CONTEXT
+
EXPLICIT TOOL SET
```

Полный универсальный CONTEXT POLICY framework пока не реализован, но этот принцип уже используется архитектурно.

---

# 14. Compressor Tools

Для Context Compressor действует:

```text
TOOLS = NONE
```

Compressor не должен:

```text
читать файлы через agent tools
изменять Workspace
запускать команды
управлять Git
менять Context Storage напрямую
```

Его задача:

```text
TEXT → COMPRESSED TEXT
```

---

# 15. Compressor Context

Текущий Compressor runtime может собирать:

```text
ROLE CONTEXT
PROJECT CONTEXT
MESSAGE COMPRESSION TEMPLATE
RUNTIME TARGET
SOURCE TEXT
optional FINAL CHECK
```

При этом действует фундаментальное правило:

```text
RAW SOURCE = SOURCE OF TRUTH
PROJECT CONTEXT = REFERENCE ONLY
```

Compressor не должен переносить в summary сведения, существующие только в Project Context.

---

# 16. Reduction Target

Compressor поддерживает пользовательский процент сокращения.

Например:

```text
50%
```

Поддерживаются placeholders:

```text
{{REDUCTION_PERCENT}}
{{REMAINING_PERCENT}}
```

Target является ориентиром.

Сохранение смысла, ограничений, чисел, имён и причинно-следственных связей важнее точного попадания в процент.

---

# 17. Final Check

В Compressor UI существует optional:

```text
Дополнительная проверка
```

Она подключает отдельный Final Check prompt block.

Это НЕ является Independent Execution Verifier.

Текущий Final Check:

```text
часть одного Compressor request
```

Independent Execution Verifier:

```text
отдельная роль
отдельный clean call
```

Он уже реализован в `verifier_runtime.py` и используется Final Audit Agent RUN, но не встроен в Compressor pipeline.

---

# 17A. Independent Final Audit — текущая карта

Final Audit выполняется после deterministic Verification Gate и до normal SUCCESS, в том числе для no-tool RUN. Источник истины — RAW TASK и server-observed evidence; Candidate является заявлением Executor, а fresh Git state не доказывает ownership текущего RUN. Критически неполное evidence блокирует SUCCESS до Verifier call. Только valid `FINAL` PASS разрешает normal SUCCESS.

Текущий V.5 допускает до двух correction handoffs в том же RUN (`FINAL_AUDIT_CORRECTION_LIMIT = 2`) с защитой от no-progress, repeated failure и ping-pong. После исчерпания лимита следующий semantic FAIL завершает RUN с `final_audit_correction_exhausted` / `FinalAuditSemanticError`; runtime, protocol и critical evidence failures немедленно terminal. Rejected Candidate и server feedback остаются внутренними сообщениями RUN, а не persistent Chat history.

Подробные evidence fields, событийный trace, feedback, retry policy и edge cases принадлежат `13_Архитектура оперативной верификации и контроля выполнения задач.md`.

---

# 18. Compressor Soft Guard

После получения результата runtime измеряет фактическое сокращение.

Soft Guard отклоняет:

```text
empty result
unchanged result
result >= RAW length
слишком слабое сокращение
```

При неудаче возможен один corrective retry.

Guard проверяет форму результата, но не является полноценной semantic verification model.

---

# 19–35. Working Representation и Context Compression — текущая карта

`context_storage.py` хранит immutable RAW MESSAGE отдельно от MESSAGE CONTEXT STATE и Context Variants. Активное представление отдельного сообщения бывает RAW (`FULL`) либо принятая пользователем SUMMARY (`COMPRESSED`). Main Chat получает выбранное представление в исходном порядке ролей; UI показывает активный текст (для COMPRESSED может показывать модель и фактическое сокращение), а Original Viewer открывает неизменённый RAW без переключения.

Compressor создаёт Proposal от RAW. Пользователь может принять, отклонить или вручную отредактировать его; только принятое представление становится активным. Restore возвращает RAW, сохраняя прежние варианты; recompression снова начинается от RAW. Состояние и варианты переживают restart. Базовый FULL → COMPRESS → PROPOSAL → ACCEPT → COMPRESSED → ORIGINAL → RESTORE → FULL lifecycle ранее подтверждён unit tests и живым UI-тестом. Один отдельный Compressor call, soft guard и максимум один corrective retry составляют текущий base lifecycle; независимые COMPRESS → VERIFY → FINALIZE, Review Chat и автоматический context-budget manager остаются будущими.

Подробные структуры Workspace / Chat / MESSAGE CONTEXT STATE, сборка Working Chat Context, UI/trace и persistence принадлежат `03_Чаты, проекты и контекст.md`. Compression lifecycle, Proposal и будущий pipeline принадлежат `Архитектурное решение - управляемое сжатие контекста.md`. Model Assignment принадлежит `11_Мультимодельная архитектура и назначение LLM.md`.

---

# 36. Что уже реализовано

На текущем этапе работают:

```text
Workspace storage
Workspace registry
Chat storage
Persistent RAW HISTORY
Project Context
Project Context revisions
multi-turn Chat history
MODEL REGISTRY
GigaChat Multi-Model
MAIN CHAT model selection
assistant producer provenance
Role Context
independent Compressor runtime
Compressor model selection
Verifier Model Assignment
one-shot clean Verifier Runtime
shared low-level GigaChat transport
typed VerifierResult PASS / FAIL
Independent Final Audit before SUCCESS
Final Audit server evidence
Evidence Completeness Gate
structured Server Final Verifier Feedback
bounded V.5 same-RUN correction lifecycle (до двух feedback handoffs)
active `final_audit.feedback` server-context event
Final Audit correction events
Compression templates
Reduction target
Final Check prompt
Compressor soft guard
per-message MESSAGE CONTEXT STATE
Context Variants
RAW / SUMMARY Working Representation
Compression Proposal
manual proposal editing
Accept
Reject
Manual Working Representation edit
Original Viewer
Restore RAW
recompression from RAW
MAIN CHAT Working Context substitution
restart persistence
trace context counters
SAFE scopes
backup
Verification Gate
GUARD P1
RUN logs
```

---

# 37. Что пока НЕ реализовано

Пока отсутствуют:

```text
Compression Review / Discussion Chat
Универсальный Task Contract DSL сверх реализованного V.6 V1
Preflight Task Contract Verification
Checkpoint Runtime
Operation IDs lifecycle
Staged Write / Staged Delete
Pre-Mutation LLM Gate
Backup Coverage Invariant / existed_before state
Persistent Backup Retention Manager
Backup History / Restore API
универсальный Agent Profile runtime
полный CONTEXT POLICY framework
Local Provider
production Ollama/local LLM integration
Workspace default → Chat override model assignment
Multi-Agent Coordinator
automatic context-budget manager
automatic compression near token limit
Chat deletion
automatic .venv / dependency deployment
Voice / ASR
Web UI
```

---

# 38. Compression Review / Discussion — следующий слой

Для COMPRESSION PROPOSAL запланирован message-scoped review mechanism.

Предварительная структура:

```text
RAW SOURCE
+
CURRENT PROPOSAL
+
REVIEW CHAT
```

При открытии Review система должна уметь сравнить:

```text
что было
```

с:

```text
что получилось
```

и выявлять:

```text
потерянные факты
потерянные числа
потерянные условия
ослабленные запреты
изменение смысла
добавленные факты
неоднозначности
```

После этого пользователь сможет обсуждать proposal с Compressor.

---

# 39. Review Chat lifecycle

Review Chat должен быть временным и привязанным к:

```text
message_id
```

Обычное сообщение в Review Chat:

```text
не должно автоматически менять Proposal
```

Отдельное действие:

```text
Сформировать новую версию
```

должно создавать новый Proposal на основе:

```text
RAW
+
CURRENT PROPOSAL
+
REVIEW DIALOGUE
```

RAW остаётся source of truth.

---

# 40. Independent Verifier

Independent Execution Verifier уже реализован как отдельная runtime role и не зависит от будущего Compressor Review-механизма.

Его задача:

```text
RAW TASK
+
SERVER-PREPARED EVIDENCE
↓
SEMANTIC CHECK
```

Текущая модель назначается отдельно от MAIN CHAT и COMPRESSOR через общий Model Registry. Default — GigaChat 3 Pro.

Каждая проверка является отдельным one-shot clean call без file tools и persistent Chat.

Фактически реализованы:

```text
V.1 Verifier Model Assignment
V.2 One-shot Clean Verifier Runtime
V.3 Independent Final Audit before SUCCESS
V.4 One-shot Final Audit Correction Handoff
V.5 Bounded Final Audit retry lifecycle
```

V.5 разрешает не более двух structured feedback handoffs в тот же Executor RUN. Каждый audit остаётся отдельным clean one-shot Verifier call с исходной RAW TASK; Verifier history между вызовами не переносится.

Permission escalation реализована в коде и покрыта tests; live пользовательский RUN не подтверждён. Mutation intent trace остаётся diagnostic-only: автоматический Execution Consistency gate отключён. Подробный lifecycle принадлежит `13_Архитектура оперативной верификации и контроля выполнения задач.md`.

Один SEND создаёт `task_block_id` для логической TASK; server-owned Audit Thread связывает её user/assistant messages, проверки, corrections, permission review и итог. Идентичности и persistent linkage принадлежат `03_Чаты, проекты и контекст.md`, execution observability — `13_Архитектура оперативной верификации и контроля выполнения задач.md`.

---

# 41. Agent Profiles

Будущий Agent Profile должен объединять:

```text
role
model_assignment
role_context
context_policy
tool_set
permission_policy
state
execution_rules
```

MAIN CHAT и COMPRESSOR уже частично демонстрируют эту структуру, но универсальный Agent Profile runtime пока не реализован.

---

# 42. Provider architecture

Текущий production provider:

```text
GigaChat API
```

Полный универсальный Provider Adapter abstraction ещё не завершён.

Будущий runtime должен позволять:

```text
GigaChat
Local Provider
другой внешний API
```

при сохранении одного общего Agent Runtime.

---

# 43. Local Provider

Local Provider пока не является production-функцией.

В будущем возможно подключение:

```text
Ollama
другого local inference runtime
```

Локальная модель не должна становиться владельцем Context или State.

---

# 44. Per-Chat Model Assignment

Полноценная схема:

```text
Workspace default model
↓
optional Chat override
```

пока не реализована.

Текущий MAIN CHAT selector уже создаёт базу для дальнейшего развития этого направления.

---

# 45. Coordinator

Multi-Agent Coordinator пока не реализован.

Целевая логика:

```text
Agent A
↓
Runtime / Coordinator
↓
selected result
↓
Agent B
```

Передача контекста между Agent должна быть explicit.

---

# 46. Safety scopes

Runtime контролирует permission scopes.

Основные категории:

```text
READ
WRITE
DELETE
VERIFY
```

Модель не может сама расширять разрешённый scope.

---

# 47. Backup

Backup session создаётся до первого model call. Для существующего файла физический backup сохраняется до первой mutation; новые файлы фиксируются в manifest.

Portable state:

```text
.ultra
```

и machine-local backup storage разделены.

Перед физической mutation существующего файла обязателен protected backup.

---

# 48. Verification Gate

После mutation runtime требует проверку результата.

Детерминированно требуются `python_compile` после изменения существующего Python-файла и `ui_smoke_test` после изменения `server.py` / `ultra_ui.py`. Остальные инструменты могут использоваться для диагностики:

```text
git_diff
git_status
python_compile
ui_smoke_test
другие verification tools
```

SUCCESS не должен считаться подтверждённым только потому, что модель написала текстовый отчёт.

---

# 49. VERIFY

Verification tools могут включаться и выключаться через runtime policy.

При:

```text
VERIFY = OFF
```

проверочные tools не должны оставаться доступными.

При:

```text
VERIFY = ON
```

runtime может требовать соответствующие checks.

---

# 50. GUARD P1

GUARD P1 является отдельным server-side behavior layer.

Он может остановить подозрительное действие до физического tool execution.

GUARD не заменяет:

```text
Permissions
Backup
Verification
```

---

# 51. No-tool completion и Final Audit

Базовый deterministic Verification Gate сам по себе не доказывает выполнение изменяющей TASK в сценарии:

```text
изменяющая TASK
↓
модель не вызывает tools
↓
stop
↓
пишет, что задача выполнена
```

Final Audit теперь выполняется и при `tool_call_count = 0` / `write_revision = 0`. Он может semantic FAIL заблокировать неподтверждённое заявление Executor о выполнении TASK.

При этом нулевое количество tools само по себе не является deterministic FAIL: read-only или explanatory TASK может завершиться корректно без mutations.

---

# 52. Голосовая подсистема

Архитектурное направление голосового ввода зафиксировано в:

12_Архитектура голосового ввода и распознавания речи

Предварительная схема:

```text
VOICE
↓
ASR MODEL
↓
TEXT
↓
CHAT INPUT
```

Первый кандидат для исследования:

```text
GigaAM v3
```

Возможный будущий кандидат:

```text
Whisper
```

Voice / ASR пока не реализован.

---

# 53. Текущие статусы основных этапов

```text
06.1
Модель данных Chat и хранение
ГОТОВО

06.2
Навигация по Chat
В РАБОТЕ
Основные функции готовы, удаление Chat отсутствует

06.3
Настоящий multi-turn context
ГОТОВО

06.4
RAW HISTORY / WORKING REPRESENTATION
ГОТОВО

06.5
Управляемое Context Compression
В РАБОТЕ
Базовый production lifecycle уже работает;
Compression Review Chat ещё отсутствует

MM.0
Архитектурное решение мультимодельности
ГОТОВО

MM.1
MODEL REGISTRY
ГОТОВО

MM.2
GigaChat Multi-Model
ГОТОВО

MM.3
MAIN CHAT MODEL ASSIGNMENT
ГОТОВО

MM.4
Independent Compressor Model Assignment / Runtime
ГОТОВО

V.1
Verifier Model Assignment
DONE

V.2
One-shot Clean Verifier Runtime
DONE

V.3
Independent Final Audit before SUCCESS
DONE

V.4
One-shot Final Audit Correction Handoff
DONE

V.5
Generalized bounded Final Audit retry engine
DONE

V.6
Task Planner / Persistence Contract V1
DONE — automated tests; live GigaChat RUN pending
```

MM.5+ остаются будущими этапами. Актуальные статусы синхронизированы с `04_Дорожная карта разработки.md` и `05_Реестр задач.md`.

---

# 54. Текущий ближайший функциональный рубеж

Ближайшее развитие Context Compression:

```text
COMPRESSION PROPOSAL
↓
Проверить / обсудить
↓
RAW vs CURRENT PROPOSAL review
↓
message-scoped temporary Chat
↓
Сформировать новую версию
↓
новый Proposal
↓
Accept
```

Execution Verifier V.1–V.5 и V.6 V1 Task Planner / Persistence Contract реализованы. Checkpoint runtime и независимый Pre-Mutation Verifier остаются будущими. Автоматический Execution Consistency остаётся отключённым.

---

# 55. Главный архитектурный принцип

Система развивается не как набор отдельных чатов внутри конкретных LLM.

Источник состояния находится в runtime.

Канонически:

```text
MODEL
    = вычислительный ресурс

ROLE
    = назначение и правила

CONTEXT
    = явно собранные данные

TOOLS
    = явно разрешённый набор

STATE
    = persistent состояние runtime

PROVIDER SESSION
    ≠ источник истины
```

---

# 56. Канонические инварианты

1. RAW MESSAGE после создания не переписывается.

2. Compression не уничтожает RAW.

3. Context Variant хранится отдельно.

4. Proposal не активируется без решения пользователя.

5. Restore возвращает active representation к RAW.

6. Recompression выполняется от RAW.

7. Project Context не равен Chat History.

8. Role Context не равен Project Context.

9. MAIN CHAT, COMPRESSOR, PLANNER и VERIFIER имеют независимые runtime calls.

10. Tools принадлежат роли/runtime, а не модели.

11. Model selection не должен менять safety policy.

12. Provider-side hidden history не является системой памяти проекта.

13. Workspace и Chat state должны переживать restart.

14. MAIN CHAT должен получать active Working Representation.

15. Original Viewer должен всегда позволять увидеть immutable RAW.

16. Candidate Final Response не является authoritative fact до Final Verifier PASS.

17. Fresh Git state нельзя автоматически приписывать текущему RUN без подтверждения `mutation_facts`.

18. Только VALID FINAL VERIFIER PASS разрешает normal RUN SUCCESS.

19. Первый valid semantic Final Audit FAIL блокирует SUCCESS, но не завершает RUN: допускается ровно один same-RUN correction handoff.

20. Rejected Candidate и Server Final Verifier Feedback не становятся обычными persistent Chat messages.

21. Повторный semantic FAIL после использованного handoff означает `final_audit_correction_exhausted` и terminal RUN FAILED.

---

# 57. Итоговое текущее состояние

`ag_llm` уже имеет рабочий фундамент локальной агентской среды:

```text
Workspace
Chat
RAW HISTORY
Project Context
Role Context
MODEL REGISTRY
GigaChat Multi-Model
MAIN CHAT model selection
Compressor model selection
Independent Compressor runtime
Verifier Model Assignment
One-shot Clean Verifier Runtime
Shared GigaChat transport
Independent Final Audit
Final Audit Evidence
Evidence Completeness Gate
One-shot Final Audit Correction Handoff
Server Final Verifier Feedback
Working Representation
Context Variants
RAW / SUMMARY switching
Compression Proposal
Restore
Permissions
Backup
Verification
Trace
```

Следующая стадия проекта — не перестройка основы, а постепенное добавление специализированных runtime roles и управляемых контекстов вокруг уже существующей системы.

Ближайшие отдельные направления:

```text
Compression Review / Discussion
Развитие Task Contract: checkpoints / Pre-Mutation verification
Agent Profile / Context Policy
Local Provider
Coordinator
Voice / ASR
```

Точная последовательность фиксируется в:

- 04_Дорожная карта разработки
- 05_Реестр задач

---

# Главная идея

```text
МОДЕЛЬ МОЖНО ЗАМЕНИТЬ.

WORKSPACE,
CHAT,
КОНТЕКСТ,
ПАМЯТЬ,
TOOLS,
PERMISSIONS,
STATE,
BACKUP,
VERIFICATION
И TRACE

ОСТАЮТСЯ ПОД КОНТРОЛЕМ AGENT RUNTIME.
```
