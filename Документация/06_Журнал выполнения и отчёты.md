# Журнал выполнения и отчёты GigaChat Ultra Local Agent

Этот документ содержит хронологию выполненных работ, отчёты по TASK и диагностику. Записи ведутся в обратном хронологическом порядке (последние сверху).

---
## 2026-09-20 — TASK DOC-MULTI-SYNC-03

### Цель

Финально закрепить CONTEXT POLICY, явную сборку контекста и изоляцию ролей / RUN перед началом runtime-реализации MM.1.

### Фактически выполнено

- в канонический документ 11 добавлены CONTEXT POLICY, ROLE CONTEXT, ALLOWED SHARED CONTEXT, RUN CONTEXT и формула MODEL CALL;
- закреплены context isolation, provider session isolation и DEFAULT DENY для tools;
- Model Selector определён только как выбор MODEL без переноса context, tools, permissions или state;
- AGENT PROFILE / MM.7 дополнен `context_policy`, `tool_set` и `permission_policy` без изменения статуса `ЗАПЛАНИРОВАНО`;
- передача между Agent описана как `RESULT → Agent Runtime / Coordinator → явно сформированный RUN CONTEXT`;
- pipeline COMPRESS → VERIFY → FINALIZE уточнён как три context-isolated вызова с отдельным payload;
- PROJECT CONTEXT разрешён компрессору только как явно подключённый reference context, а RAW сохранён источником истины для summary;
- точечно синхронизированы документы 03, 04, 05 и 08.

### Статус реализации

CONTEXT POLICY, AGENT PROFILE, PROJECT ORIENTATION, MODEL REGISTRY, Provider Adapter и Coordinator остаются архитектурными решениями и в runtime не реализованы.

### Проверено в этом RUN

- перечитаны изменённые документы и ключевые архитектурные разделы;
- проверены критерии изоляции контекста, provider session и tools;
- выполнены read-only `git diff --check`, `git diff` и `git status`;
- runtime-код не изменялся; Python compile, UI smoke test и restart не требовались.

---
## 2026-09-20 — TASK DOC-MULTI-SYNC-01

### Цель

Синхронизировать проектную документацию с принятым документом 11_Мультимодельная архитектура и назначение LLM, не изменяя runtime-код.

### Фактически выполнено

- документ 11 принят как каноническое мультимодельное архитектурное решение;
- синхронизированы фактически изменённые документы проекта;
- зафиксирована отдельная ветка Roadmap и реестра `MM.0–MM.8` без перенумерации TASK 06, 07 и 08;
- согласована терминология MODEL REGISTRY / PROVIDER / MODEL ASSIGNMENT;
- архитектура Context Compression синхронизирована с тремя отдельными проходами COMPRESS → VERIFY → FINALIZE;
- явно зафиксировано, что текущий runtime по-прежнему использует `MODEL = "GigaChat-3-Ultra"`, а MODEL REGISTRY, Provider Adapter abstraction и MODEL ASSIGNMENTS ещё не реализованы.

### Проверено в этом RUN

- перечитаны источники истины и все реально изменённые документы;
- выполнены проверки согласованности терминологии и статусов;
- проверены Git diff и Git status только read-only командами;
- runtime-код не изменялся; Python compile и UI smoke test не требовались.

---
## 2026-09-19 — Нормализация канонического журнала

### Причина

В ходе DOC-SYNC был обнаружен конфликт двух почти одинаковых имён.

Канонический файл:

`06_Журнал выполнения и отчёты.md`

Ошибочный дубль:

`06_Журнал выполнения и отчётов.md`

Из-за различия в окончании слова `отчёты / отчётов` новые записи несколько раз попадали в ошибочный дубль вместо канонического журнала.

### Решение

Каноническим документом проекта является только:

`Документация/06_Журнал выполнения и отчёты.md`

Ошибочный вариант:

`Документация/06_Журнал выполнения и отчётов.md`

не должен существовать в финальной структуре документации.

### Архитектурный вывод

Этот инцидент стал одним из реальных сценариев для разработки и тестирования GUARD P1.

---

## 2026-09-19 — ОБНАРУЖЕННЫЙ GAP: NO-TOOL STOP

RUN:

`20260919_215131_7e4717`

### Наблюдаемый факт

RUN завершился:

```text
API #1 -> stop
SUCCESS
TOOLS: 0
```

При этом Ultra выдала финальный отчёт так, будто документы были прочитаны, изменены и проверены.

По фактическому trace этого не происходило.

### Архитектурное значение

Текущий Verification Gate контролирует корректность проверок после реальных файловых mutations.

Если модель вообще не вызывает tools:

- `write_revision` остаётся равным 0;
- Verification Gate не видит незавершённой файловой mutation;
- RUN может завершиться `stop / SUCCESS`;
- модель при этом способна текстом заявить о невыполненных действиях.

### Статус

Проблема ОБНАРУЖЕНА.

Автоматическая защита от такого поведения пока НЕ РЕАЛИЗОВАНА.

Рабочее название возможного будущего слоя:

`TASK EXECUTION GATE / NO-TOOL STOP GUARD`

Он должен отдельно контролировать ситуацию, когда изменяющая TASK завершается без необходимых фактических tool actions.

---

## 2026-09-19 — GUARD P1: SERVER-SIDE SUPERVISOR

GUARD P1 реализован как первый server-side behavior guard.

Основные runtime-файлы:

- `server.py`;
- `ultra_ui.py`.

В UI существует отдельная настройка `GUARD P1` рядом с `VERIFY`.

По умолчанию GUARD P1 включён.

### Назначение

GUARD P1 не заменяет permission system и Verification Gate.

Его задача — вмешиваться ДО выполнения подозрительного действия и возвращать модели короткий внешний `SUPERVISOR CHECK`.

Модель получает возможность перечитать TASK, предыдущие результаты и изменить собственную траекторию выполнения.

### Физический тест 1 — REPEATED READ

RUN:

`20260919_212213_00575f`

Наблюдаемый trace:

```text
TOOL #1 read_file -> OK
TOOL #2 read_file -> OK
GUARD P1 | SUPERVISOR CHECK | REPEAT read_file
```

Третья идентичная попытка `read_file` не была физически выполнена.

Итог:

```text
TOOLS: 2
SUCCESS
```

Ultra после `SUPERVISOR CHECK` прекратила бессмысленные повторные чтения.

### Физический тест 2 — POSSIBLE DUPLICATE CREATE

RUN:

`20260919_213415_15e109`

Существующий файл:

`06_Журнал выполнения и отчёты.md`

Имитация ошибочного CREATE:

`06_Журнал выполнения и отчётов.md`

Наблюдаемый результат:

- существующий файл был найден и прочитан;
- попытка CREATE почти одинакового файла была перехвачена ДО записи;
- сервер выдал `SUPERVISOR CHECK | CREATE`;
- similarity score составил `145`;
- ошибочный новый файл не был создан;
- Ultra вернулась к существующему каноническому пути;
- изменение существующего файла было выполнено;
- `git_diff` и `git_status` выполнены;
- `VERIFY GATE -> PASSED`;
- RUN завершился `SUCCESS`.

### Известное ограничение GUARD P1

Позднее обнаружен важный edge case.

Если подозрительное имя нового файла буквально присутствует в тексте TASK как пример или описание старой ошибки, текущая логика может интерпретировать это как возможное явное разрешение пользователя на CREATE.

Из-за этого защита от near-duplicate CREATE требует дальнейшего уточнения.

### Текущий статус GUARD P1

Физически подтверждены:

- вмешательство при повторных успешных deterministic reads;
- вмешательство при одном типе suspicious near-duplicate CREATE.

GUARD P1 является v1 и НЕ является универсальной системой понимания намерений TASK.

Он не заменяет:

- READ / WRITE / DELETE scopes;
- permission gate;
- backup;
- Verification Gate;
- ручной review trace.

Динамическое увеличение tool budget по запросу модели пока НЕ РЕАЛИЗОВАНО.

---

## 2026-09-19 — TASK DOC-SYNC-02A: Roadmap + Task Registry

Успешный RUN:

`20260919_215423_a583ea`

### Цель

Синхронизировать с фактической реализацией:

- `04_Дорожная карта разработки.md`;
- `05_Реестр задач.md`.

### Фактический trace

```text
read 04
read 05
write 04
write 05
read 04
read 05
git_diff
git_status
VERIFY GATE -> PASSED
SUCCESS
TOOLS: 8
```

### Основные исправления

- устранено использование `CRUD` там, где Delete Chat отсутствует;
- TASK 06.2 оставлена в статусе `В РАБОТЕ`;
- TASK 06.3 больше не заявляет отдельный специальный long-dialog acceptance test;
- TASK 06.4 отделяет уже существующие RAW HISTORY / Active Chat Context от будущих WORKING REPRESENTATION / SUMMARY / trimming;
- TASK 06.5 оставлена `ЗАПЛАНИРОВАНО`;
- для этапа 07 разделены portable Workspace state и local per-machine runtime state;
- старое `logs/runs` больше не используется в этих документах как описание текущего размещения RUN logs.

### Результат

Roadmap и Registry приведены к текущему фактическому состоянию системы.

---

## 2026-09-19 — TASK DOC-SYNC-01: первый большой проход синхронизации документации

### Цель

Привести основную документацию GigaChat Ultra Local Agent ближе к фактическому состоянию проекта после большого объёма runtime-разработки.

### Затронутые документы

- `00_Главная - карта проекта.md`;
- `01_Архитектура и текущее состояние.md`;
- `03_Чаты, проекты и контекст.md`;
- `04_Дорожная карта разработки.md`;
- `05_Реестр задач.md`.

### Что было сделано

Первым большим проходом были актуализированы:

- HUB проекта;
- описание архитектуры;
- Workspace / Chat / Context;
- Roadmap;
- Task Registry.

### Важное историческое замечание

Именно во время DOC-SYNC-01 появилась ошибка имени журнала.

Ошибочный вариант:

`06_Журнал выполнения и отчётов.md`

вместо канонического:

`06_Журнал выполнения и отчёты.md`

Из-за этого часть отчёта первоначально оказалась в отдельном почти одноимённом документе.

### Последующий review

После DOC-SYNC-01 отдельный аудит обнаружил несколько переоценённых или устаревших утверждений.

Поэтому DOC-SYNC-01 нельзя считать финальной синхронизацией документации.

Исправления продолжаются небольшими отдельными TASK.

---

## 2026-09-18 — TASK DOC-04: Обязательный регламент Ultra и автоматическая загрузка правил

### Цель

Создать единый обязательный регламент поведения Ultra и обеспечить его автоматическую загрузку в system prompt каждого вызова run_agent_task. Пользователь больше не должен вручную просить Ultra соблюдать базовые правила — они действуют по умолчанию.

### Изменённые файлы

- Документация/00_Обязательный регламент Ultra.md — создан
- server.py — изменён
- Документация/00_Главная - карта проекта.md — обновлена
- Документация/01_Архитектура и текущее состояние.md — обновлена
- Документация/02_Правила разработки и работы с Ultra.md — обновлена
- Документация/06_Журнал выполнения и отчётов.md — этот файл

### Что реализовано

1. Создан документ «00_Обязательный регламент Ultra.md» со статусом ОБЯЗАТЕЛЬНО, версия 1.0, содержащий 16 разделов правил.
2. В server.py реализована автоматическая загрузка регламента при каждом вызове run_agent_task:
   - путь определяется относительно app_dir = Path(__file__).resolve().parent
   - регламент читается с диска на старте каждого run, а не при import
   - при отсутствии или пустом файле — fail-closed: RuntimeError и остановка
   - текст регламента добавляется в system message отдельным блоком «ОБЯЗАТЕЛЬНЫЙ РЕГЛАМЕНТ ULTRA»
3. Сигнатура run_agent_task не изменена.
4. ultra_ui.py не изменялся.
5. В карту проекта добавлена ссылка на новый регламент.
6. В архитектуру добавлено описание механизма загрузки регламента.
7. В правила разработки добавлено пояснение о расположении полного регламента.

### Проверка

- Путь к регламенту не содержит абсолютных Windows-путей (M:\\ и т.п.), используется Path(__file__).resolve().parent.
- Регламент читается при каждом run_agent_task, а не кэшируется при import.
- При отсутствии файла Документация/00_Обязательный регламент Ultra.md запуск блокируется с понятной ошибкой.
- Существующий system prompt file-agent сохранён, регламент добавлен как отдельный блок.
- Сигнатура run_agent_task(task, workspace_root, *, on_event=None) сохранена.
- ultra_ui.py не изменялся.
- После изменения server.py требуется перезапуск приложения.

### Результат

Ultra теперь автоматически получает обязательный регламент в system context каждого запуска. Пользователь не должен вручную просить соблюдать базовые правила. Отсутствие регламента блокирует запуск (fail-closed).

### Известные проблемы

- TASK 06.1–06.5 ещё не реализованы в коде, только запланированы.
- Настоящий multi-turn контекст отсутствует.
- Web UI не реализован.
- Переносимость запуска требует доработки.

### Следующий шаг

Начать реализацию TASK 06.1 — модель данных чатов и хранение на диске.

### Требуется restart

Да. После изменения server.py требуется полный перезапуск приложения.

---

## 2026-09-18 — TASK DOC-02: Упрощение системы нумерации и реестра задач

### Цель

Упростить систему нумерации задач в документации проекта, перейдя от детальной исторической нумерации (05A, 05A-1, 05A-1.5, 05A-2, 05A-3, 05A-3.1, 05A-3.2) к более простой системе крупных этапов (05, 06, 07, 08). Исторические подэтапы 05A* объединены в завершённый этап 05.

### Изменённые файлы

- Документация/05_Реестр задач.md — объединён этап 05, заменены 06A–06E на 06.1–06.5
- Документация/04_Дорожная карта разработки.md — заменены 06A–06E на 06.1–06.5

### Что реализовано

1. Этап 05 представлен одной завершённой записью: «Базовая инфраструктура локального Ultra — ГОТОВО»
2. Исторические подэтапы 05A* удалены из рабочего реестра как отдельные TASK
3. Этап 06 перенумерован: 06A→06.1, 06B→06.2, 06C→06.3, 06D→06.4, 06E→06.5
4. Смысл и критерии приёмки подэтапов 06 полностью сохранены
5. Этапы 07 и 08 сохранены без изменений
6. В журнале добавлено пояснение об упрощении нумерации

### Проверка

- В 05_Реестр задач.md этап 05 — одна строка, 06.1–06.5 соответствуют старому смыслу 06A–06E
- В 04_Дорожная карта разработки.md нумерация 06.1–06.5, буквенная система A/B/C/D/E устранена
- Исходный код НЕ изменялся
- Ссылки между документами сохранены

### Результат

Система нумерации упрощена. Рабочий реестр не перегружен историческими подэтапами. Новые задачи используют простую числовую систему 06.1–06.5. Исторические сведения сохранены для понимания происхождения изменений.

### Известные проблемы

- TASK 06.1–06.5 ещё не реализованы в коде, только запланированы
- Настоящий multi-turn контекст отсутствует
- Web UI не реализован
- Переносимость запуска требует доработки

### Следующий шаг

Начать реализацию TASK 06.1 — модель данных чатов и хранение на диске.

---

## 2026-09-18 — TASK DOC-01: Создание системы документации проекта

### Цель

Создать целостную систему проектной документации для GigaChat Ultra Local Agent, которая станет основной точкой управления дальнейшей разработкой.

### Изменённые файлы

- Документация/00_Главная - карта проекта.md
- Документация/01_Архитектура и текущее состояние.md
- Документация/02_Правила разработки и работы с Ultra.md
- Документация/03_Чаты, проекты и контекст.md
- Документация/04_Дорожная карта разработки.md
- Документация/05_Реестр задач.md
- Документация/06_Журнал выполнения и отчётов.md (этот файл)
- Документация/07_Шаблоны задач, отчётов и решений.md

### Что реализовано

1. Создан главный HUB-документ: 00_Главная - карта проекта
2. Описана фактическая архитектура и текущее состояние системы
3. Сформулированы правила разработки и работы с Ultra
4. Описана целевая модель чатов, проектов и контекстов
5. Зафиксирована дорожная карта (TASK 06A–06E, 07, 08)
6. Создан рабочий реестр задач со статусами
7. Подготовлен формат журнала выполнения
8. Созданы шаблоны для единообразия документации
9. Существующий документ по сжатию контекста сохранён и интегрирован

### Проверка

- Все документы созданы в папке Документация/
- Использованы Obsidian-ссылки в двойных квадратных скобках для навигации
- Названия на русском языке
- Текущее состояние отделено от планов
- Исходный код НЕ изменялся
- Существующий документ по сжатию контекста прочитан, тестовый артефакт "ULTRA UI WRITE WORKS" удалён из начала файла

### Результат

Система документации готова к использованию. Папку «Документация» можно открыть как отдельный Obsidian Vault. Все ключевые аспекты проекта задокументированы: архитектура, правила, планы, задачи.

### Известные проблемы

- TASK 06A–06E ещё не реализованы в коде, только запланированы
- Настоящий multi-turn контекст отсутствует
- Web UI не реализован
-  переносимость запуска требует доработки

### Следующий шаг

Начать реализацию TASK 06A — модель данных чатов и хранение на диске.

---

## Исторический итог (инфраструктура)

К текущему моменту реализована базовая инфраструктура:

- **standalone Ultra** — автономное приложение
- **Tkinter UI** — графический интерфейс
- **file tools** — контролируемый доступ к файловой системе
- **trace/event subsystem** — логирование действий
- **runtime warning** — предупреждение об изменениях
- **sidebar лога** — визуальный мониторинг
- **успешный диагностический run** — проверка работоспособности

Эта инфраструктура является фундаментом для дальнейших этапов (06.1–06.5).

**Примечание по нумерации:** Система нумерации задач упрощена. Исторические TASK 05A* объединены в завершённый этап 05 — «Базовая инфраструктура Ultra». Новые задачи используют упрощённую систему 06.1–06.5 вместо 06A–06E. Исторические записи сохраняются для понимания происхождения изменений.
